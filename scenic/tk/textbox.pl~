#!/usr/bin/perl -w

use strict;
use Tk;
use utf8;
use encoding 'utf8';

# Main Window
my $mw = MainWindow->new;
$mw->title("漢字");
$mw->minsize(qw(275 150));

my $sample_text = $mw->Text()->pack;

$text->insert('end',"To be or not to be\nThat is the question");
