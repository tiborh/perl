#!/usr/bin/perl -w

use strict;

#diamond (<>) primarily reads @ARGV, but if that's empty, it works on the ones below too.)
#<STDIN> reads standard input, redirections to stdinput (<filename), or pipelines (|)
print <>;
