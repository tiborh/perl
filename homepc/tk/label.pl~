#!/usr/local/bin/perl -w

use Tk;

&come_window;

sub come_window {
    my $mw = MainWindow->new;
    $mw->configure(
	-title => "Geometry experiments", 
	-background => "SlateBlue1", 
	-width => "300", 
	-height => "100",
	);
    $mw->geometry("+100+300");
    MainLoop;
}
